# 업무 책갈피 — 개발 계획 및 수용시험 v0.1

작성일: 2026-09-18  
기준 명세: `01_IMPLEMENTATION_SPEC.md`  
현재 상태: 계획 문서. 아래 실제 실행 결과는 모두 **미실행**이다.

## 1. 진행 방식

개발은 P0→P1→P2→P3→P4 순서로 진행한다. 기능별 검증이 통과하면 다음 단계로 이어간다. 일상적인 구현 선택마다 사용자에게 재승인을 받지 않는다. 기술 검증 실패 시 원인·증거·대안을 기록하고 독립 가능한 작업은 계속한다.

다만 실패한 필수 기능을 제거하고도 ‘v1 완료’라고 표시해서는 안 된다. 환경별로 `통과`, `실패`, `미실행`, `차단`을 구분한다. 플랫폼이 없어 실행하지 못한 것은 실패가 아니라 미실행이다.

## 2. 단계별 산출물과 통과 기준

| 단계 | 해야 할 작업 | 산출물 | 다음 단계 판단 |
|---|---|---|---|
| P0: 위험 검증 | 탐색기 활성 탭, Excel foreground 연결, 닫힌 파일 셀 재개, worker 시간초과 PoC | `docs/feasibility-report.md`, 최소 probe 코드, 빌드/버전 기록 | 잘못된 대상 저장 0건. 핵심 경로의 실제 성공 확인 |
| P1: 공통 골격 | Core 모델, 단일 실행, 단축키, SQLite, worker 프로토콜, 상태 코드 | solution, 의미 있는 단위/장애 시험 | 저장 성공/실패의 정합성, 반복 요청·늦은 응답 차단 |
| P2: 탐색기 전체 흐름 | 저장·최근 목록·Enter 재개, 파일 종류 정책, 메모·삭제·경로 수정 | 탐색기 시험판 | E 계열과 U/D 중 탐색기·공통 시험 통과. Excel 의존 항목은 P3/P4에서 검증 |
| P3: Excel 전체 흐름 | 다중 인스턴스 캡처·열린/닫힌 문서 재개·부분 성공 | Excel 포함 시험판 | X 계열 필수 시험 통과, 원본 금지 명령 없음 |
| P4: 패키징과 업무 PC 확인 | self-contained ZIP, 재현 빌드, 한국어 안내, 실제 호환성/자원 측정 | 실행 패키지, 소스, 해시, 검증 보고서 | 필수 Gates 통과. 미검증 환경은 지원표에 표시 |

P0에서는 예쁜 UI를 먼저 만들지 않는다. 작은 probe로 정확한 경로·시트·셀 DTO를 확인한다. P1의 DB/Core 개발은 P0의 호환성 시험과 병행할 수 있다.

### P0의 구체적 실험

| 실험 | 절차 | 통과 조건 |
|---|---|---|
| P0-A 탐색기 탭 | 폴더 A/B 탭을 만들고 선택을 다르게 한다. 같은 폴더의 두 탭에서도 서로 다른 파일을 선택한다. 창 2개를 오가며 총 50회 캡처한다 | 모든 정상·안정 상태 시도가 기대 경로와 일치. 전환 중 불명확 상태는 명시적으로 실패. 다른 탭 경로 저장 0건 |
| P0-B Excel 연결 | 서로 다른 폴더에 같은 이름의 파일을 둔다. 같은 프로세스의 여러 창과 별도 프로세스를 번갈아 활성화해 50회 캡처한다 | 안정 상태에서 실제 파일·시트·셀 일치. 이름만 같은 다른 파일 선택 0건 |
| P0-C 닫힌 문서 재개 | 비매크로 fixture의 특정 셀을 기록하고 Excel을 사용자가 정상 종료한다. 재개한다 | 정확한 파일을 열고 시트·셀을 확인. 기존 파일 저장·숨은 사본 생성 없음 |
| P0-D 응답 지연 | worker 지연을 주입하고 실제 Excel 편집/대화상자 중 호출한다 | UI 응답 유지, 제한시간 적용, 자체 worker만 정리, 늦은 성공 반영 없음 |

매번 지원 불가로 돌려 잘못된 저장 0건만 달성하는 것은 통과가 아니다. 지원할 정상 환경에서는 정확한 성공 경로를 실증해야 한다.

## 3. 권장 저장소 구성

| 경로 | 용도 |
|---|---|
| `src/WorkBookmark.App/` | WinForms 트레이·목록·메모·설정 및 worker 모드 진입점 |
| `src/WorkBookmark.Core/` | 모델·서비스·결과 타입·정책 |
| `src/WorkBookmark.Windows/` | P/Invoke·Shell·Excel·STA 연동 |
| `src/WorkBookmark.Storage/` | SQLite repository·migration·일관된 백업 |
| `tests/WorkBookmark.Core.Tests/` | 경로·중복·순서·상태 전이·메모 정책 |
| `tests/WorkBookmark.Integration.Tests/` | DB 장애·IPC·worker 종료·Windows API 시험 |
| `tools/Probes/` | P0용 재현 가능한 최소 실험 코드 |
| `testdata/` | 비식별 fixture와 생성 절차. 실제 업무 문서 금지 |
| `docs/` | 본 명세, 검증 결과, 호환성표, 사용자 안내, 결정 기록 |
| `scripts/` | 재현 가능한 빌드·패키지 스크립트 |

초기 프로젝트 이름은 제안이다. 동일 책임을 명료하게 유지하면 불필요하게 프로젝트 개수를 늘릴 필요는 없다. DI 프레임워크·플러그인 로더·웹 서버·업무 관리 계층을 먼저 구축하지 않는다.

### Core 계약의 최소 형태

| 계약 | 입력 → 출력 | 유의점 |
|---|---|---|
| `Capture` | 요청 ID + 일시 대상 스냅샷 + deadline → `CapturedTarget` 또는 원인 코드 | DB 쓰기는 App이 결과 검증 후 수행 |
| `Resume` | 요청 ID + immutable 책갈피 스냅샷 + deadline → 단계별 결과 | 이미 보낸 외부 동작/미확인 결과를 구분 |
| `UpsertCapture` | 유효한 대상 DTO → 커밋된 ID | 기존 메모 보존, 단일 트랜잭션 |
| `UpdateNote` | ID + 새 메모 → 커밋 결과 | 새 책갈피 생성과 별개 |
| `ListRecent` / `Search` | 검색어·개수 → 표시 DTO | 최근 정렬에 재개 시각 사용 금지 |
| `Relink` | ID + 사용자 지정 경로 → 검증/수정 결과 | 중복 충돌·시트 불일치 처리 |

코드 예제에 가짜 성공 결과를 넣어 UI를 완성하지 않는다. Mock은 자동시험에서만 사용하고 제품 실행 경로와 분리한다.

## 4. 시험 준비물

모든 문서는 합성 데이터다. ‘교육생 001’처럼 가상 값을 사용하고 이름·메일·실제 사내 경로를 fixture에 넣지 않는다.

| Fixture | 구성과 목적 |
|---|---|
| F-A | 다른 폴더의 `같은이름.xlsx` 2개. 시트 `확정자`, `대기자`, 서로 다른 셀 위치 |
| F-B | `한글 공백 & 괄호 (검증)` 폴더, 문서·이미지·일반 폴더, 루트 경로 |
| F-C | 사용자가 저장하지 않은 새 통합문서와, 저장 후 셀을 수정한 통합문서 |
| F-D | 다중 범위·병합 셀·숨김 행/열·필터·숨겨진/삭제된 시트·선택 제한 보호 |
| F-E | 연결 끊기 가능한 테스트 공유 폴더. 없으면 해당 시험 미실행 |
| F-F | 실행 시 표시만 남기는 무해한 테스트 스크립트와 바로가기. 자동 실행되지 않는지 확인 |
| F-G | 별도 시험 환경의 이벤트 매크로 파일. 기존 이벤트 발생과 도구의 직접 수정 명령을 구분 |
| F-H | 저장 실패·DB 손상·worker 지연/종료를 재현하는 주입 장치 |

DRM·보호된 보기·회사 정책 시험은 승인된 가상/샘플 문서로 수행한다. 차단을 해제하거나 보호 기능을 바꾸지 않는다.

## 5. 필수 수용시험

표의 `G`는 일반 v1 출시 필수, `C`는 해당 환경 지원을 주장할 때 필수다. 현재 결과는 전부 미실행이다. 단위시험은 규칙·오류 처리를 검증하고, 실제 Windows/Office 시험은 연동을 검증한다. 서로 대체하지 않는다.

### 5.1 조작과 목록

| ID | Gate | 시험 | 기대 결과 | 검증 방법 |
|---|---|---|---|---|
| U01 | G | 새 업무 시작 | 앱 안의 등록 절차 없이 원래 앱에서 일 시작 | 수동 실기 |
| U02 | G | 저장 단축키 1회 | 제목·확인 입력 없이 커밋, 알림이 foreground 유지 | 실기 + DB |
| U03 | G | 재개 단축키 + Enter | 맨 위 항목 즉시 재개, 상세 화면 없음 | 실기 |
| U04 | G | 메모 없이 저장·재개 | 경로/셀 재개에 메모가 필요하지 않음 | 실기 |
| U05 | G | 저장 후 메모 취소/실패 | 책갈피 유지, 실패를 메모 저장 성공으로 표시 안 함 | 단위 + 실기 |
| U06 | G | 같은 지점 재저장 | 항목 1개, 캡처 시각 갱신, 이전 메모와 메모 시각 유지 | 단위 + 실기 |
| U07 | G | 다른 Excel 셀 저장 | 셀별 별도 항목, 파일-only 항목과도 구분 | 단위 + 실기 |
| U08 | G | 목록 열기/재개/메모 수정 | 최근 캡처 순서 불변. 표시 중 선택이 다른 항목으로 바뀌지 않음 | 단위 + 실기 |
| U09 | G | 한글 IME·Enter·Esc | 조합 확정 Enter가 파일 열기/메모 제출까지 중복 실행 안 함 | 실기 |
| U10 | G | 삭제·되돌리기 | 책갈피만 숨김/복구, 실제 파일·메모 유지 | 단위 + 실기 |
| U11 | G | 단축키 충돌·변경 | 실패 조합 비활성 안내, 기존 유효 조합 보존, 변경 성공 시 이전 해제 | 실기 |
| U12 | G | 연타·키 길게 누르기 | 중복 외부 실행·무한 큐 없음 | 자동 + 실기 |
| U13 | G | 1,000개 기록·검색 | 최근 20개, 전체 대상 검색, 동일 이름의 경로 식별 | 자동 + 실기 |
| U14 | G | 앱 두 번 실행·종료·재실행 | 사용자별 단일 실행, 기록 유지, 단축키 정리 | 실기 |

### 5.2 탐색기

| ID | Gate | 시험 | 기대 결과 | 검증 방법 |
|---|---|---|---|---|
| E01 | G | 선택 없음/파일 1개/폴더 1개 | 각각 현재 폴더/파일/선택 폴더 기록 | 실기 |
| E02 | G | 다중 선택 | 저장하지 않고 단일 선택 안내 | 실기 + DB |
| E03 | G | 창 2개·여러 탭·동일 폴더 다른 선택 | 실제 foreground 활성 뷰만 기록 | P0-A 실기 |
| E04 | G | 탭 전환/파일 선택 변경 직후 캡처 | 정확한 기록 또는 ContextChanged. 이전 선택과 새 경로가 섞이지 않음 | 반복 실기 |
| E05 | G | 홈/최근/검색/휴지통/ZIP/바탕화면/파일 대화상자 | 지원 불가 안내, 가짜 경로 저장 없음 | 실기 |
| E06 | G | 같은 파일명·한글·공백·특수문자 | 실제 경로 보존, 파일명 기반 혼동/명령 해석 없음 | 단위 + 실기 |
| E07 | G | 문서 허용 목록과 기타 형식 | 문서는 열기 요청, 실행·스크립트·바로가기·미등록 확장자는 위치 표시 | 정책시험 + 실기 |
| E08 | G | 파일 이동/삭제/권한 거부 | 정상 성공 없음, 기존 항목/메모 유지 | 실기 |
| E09 | G | 경로 다시 지정/중복 충돌 | 사용자가 지정한 변경만 적용, 충돌 시 보존, 원본 이동 안 함 | 단위 + 실기 |
| E10 | C | UNC·매핑 드라이브·연결 끊김 | 연결 시 동작, 끊김 시 UI 응답 및 제한시간 유지 | 네트워크 실기 |
| E11 | C | 긴 경로·동기화/placeholder 파일 | 실증한 범위만 지원. 다운로드/지연 결과를 정확히 표시 | 별도 실기 |

### 5.3 Excel

| ID | Gate | 시험 | 기대 결과 | 검증 방법 |
|---|---|---|---|---|
| X01 | G | 저장된 xlsx의 시트·활성 셀 | 전체 경로 + 시트 + 절대 A1 정확히 기록 | 실기 + DB |
| X02 | G | 다중 범위·병합 셀 | 활성 셀 하나, 병합 주소 규칙 일관 | 실기 |
| X03 | G | 같은 프로세스 여러 창·별도 프로세스·동명 파일 | foreground 대상과 정확히 일치 | P0-B 실기 |
| X04 | G | 같은 통합문서의 여러 창 | 동일 통합문서임을 검증하고 문서 혼동 없이 재개 | 실기 |
| X05 | G | 미저장 새 문서 | 경로 없음으로 거절, Saved 플래그만으로 잘못 판정하지 않음 | 실기 |
| X06 | G | 저장 후 미저장 변경 | 캡처 허용·당시 상태 표시, 변경 내용 유지, Save/Close 호출 없음 | 실기 + 호출 검토 |
| X07 | G | 이미 열린 문서 재개 | 그 문서 재사용, 다른 문서 닫기·저장 없음 | 실기 |
| X08 | G | Excel 종료 후 재개 | 파일 열기 한 번, 정확한 시트·셀 검증 | P0-C 실기 |
| X09 | G | 시트 삭제/이름 변경/숨김 | 다른 시트·A1로 성공 처리 안 함, 시트 자동 생성/표시 없음 | 실기 |
| X10 | G | 필터/숨김 행·열/선택 보호 | 상태를 변경하지 않음. 보이는 위치 재개 불가를 정확히 안내 | 실기 |
| X11 | G | 편집 중·모달 창·응답 지연 | 키 입력 흉내 없음, UI 응답, 기한 내 안내, Excel 강제 종료 없음 | 실기 + 장애주입 |
| X12 | G | 차트 시트/셀 없음/지원하지 않는 경로 DTO | 지원 불가, 창 제목이나 URL 추측 변환 안 함 | 차트 시트 실기 + 경로 정책 자동시험 |
| X13 | G | 이벤트 없는 xlsm/xlsb/xls fixture | 지정한 지원 형식에서 캡처·재개. 직접 매크로 실행/전역 설정 변경 없음 | 실기 + 호출 검토 |
| X14 | G | 같은 경로가 여러 인스턴스에 열림 | 하나로 확정 안 되면 모호함 안내/예외 선택. 임의 사본 변경 안 함 | 실기 |
| X15 | G | 열린 문서 열거 일부 실패·경로 대소문자/별칭 차이 | ‘문서 없음’으로 단정해 중복 열기 안 함. 잠재 동일 후보는 확인하거나 미확인 처리 | 장애주입 + 실기 |
| X16 | G | 행 삽입·정렬 후 재개 | 저장 좌표로 이동하며 사람/데이터 추적이라고 표시 안 함 | 실기 |
| X17 | G | Excel 대신 다른 기본 연결 앱 | 열기 요청과 셀 이동을 구분, 셀 성공 없음 | 별도 테스트 계정 실기 |
| X18 | C | Excel 32비트 | 실제 지원 조합 시험 후 호환성표 기입 | 별도 실기 |
| X19 | C | DRM·암호·보호된 보기·상승 권한 앱·HTTPS 문서·이벤트 파일 | 정책 우회 없이 지원 거절/사용자 조치 필요, 기존 문서 보존. 기존 이벤트와 직접 수정 명령의 차이 확인 | 가능한 환경별 실기, 나머지 미실행 명시 |

### 5.4 저장·장애·외부 영향

| ID | Gate | 시험 | 기대 결과 | 검증 방법 |
|---|---|---|---|---|
| D01 | G | DB 커밋 직전 오류 | 저장 성공 알림 없음, 기존 기록 유지 | 장애주입 |
| D02 | G | 메모·경로 갱신 실패 | 원래 값 유지, 미커밋 값이 성공으로 남지 않음 | 장애주입 |
| D03 | G | DB 손상/마이그레이션 실패 | 자동 초기화/덮어쓰기 없음, 일관된 백업/원본 유지 | 통합시험 |
| D04 | G | 캡처 시간초과 후 늦은 성공 | 새 기록·늦은 성공 알림·최근 순서 갱신 없음 | IPC 장애주입 |
| D05 | G | 재개 시간초과 후 대상 앱이 늦게 반응 | 결과 미확인 표시, 자동 재실행·자동 종료 없음 | 장애주입 + 실기 |
| D06 | G | worker 강제 종료 | 자체 worker만 종료. Excel/Explorer/연결 앱 생존 | 통합 + 실기 |
| D07 | G | 재개 중 사용자가 다른 앱으로 이동 / 정상적인 대상 창 전환 | 사용자 이탈 후 추가 이동/포커스 명령 없음. 목록 닫힘·대상 Excel 열림·시작 키 해제는 취소로 오인하지 않음. X08도 함께 통과 | 실기 |
| D08 | G | Windows가 전면 전환 거부 | 셀 이동 확인과 화면 전환을 별도 표시 | 장애주입 + 실기 |
| D09 | G | 관리자 권한 없이 실행·시작 등록/해제 | 시스템 변경 없이 기본 기능 사용, 자기 바로가기만 삭제 | 클린 계정 실기 |
| D10 | G | 금지 API/행위 검토 | Save/SaveAs/Close/Quit/Saved setter/값쓰기/보호 해제/매크로 실행 없음 | 코드 검토 + 어댑터 경계 spy |
| D11 | G | 로그·IPC·데이터 확인 | 기본 로그에 문서 내용/메모/경로 없음, 외부 통신 없음, DB만 메타데이터 보관 | 코드/실기 검토 |
| D12 | G | 앱 재시작·PC 재시작 후 재개 | HWND 없이 경로로 재개, 메모 유지 | 실기 |
| D13 | G | 연동 100회와 장시간 유휴 | 자체 프로세스·핸들 누수 없음, 성능 목표 측정값 제시 | 성능 측정 |
| D14 | G | Excel 미설치 | 탐색기·목록 정상, Excel 좌표 재개는 지원 불가 안내 | 클린 환경 실기 |

안정적인 한 환경에서 단축키 입력과 파일 경로가 일치한 화면/진단 증거를 남긴다. 사내 문서 화면을 외부 공유용 시험 보고서에 삽입하지 않는다.

## 6. 성능과 실사용 유효성

자동/실기 시험이 통과하면 사용자가 실제 일을 5번 중단·재개해 보는 작은 파일럿을 수행할 수 있다. 별도 활동 추적기를 만들지 않고 아래만 수동으로 기록한다.

| 항목 | 평가 질문 |
|---|---|
| 입력 부담 | 제목·메모 없이 남긴 책갈피도 다시 찾는 데 도움이 되었는가? |
| 조작 수 | 시작 0회, 저장 1회, 재개 단축키+Enter가 실제로 유지되는가? |
| 정확도 | 엉뚱한 파일이나 셀을 한 번이라도 열었는가? |
| 회복 비용 | 실패 안내가 짧고 이해 가능했는가? |
| 반복 사용 | 최근 목록이 쌓여도 원하는 지점을 쉽게 찾았는가? |

파일럿은 사용성 평가이며 핵심 정확도 시험을 대체하지 않는다. 메모를 매번 써야만 도움이 된다면 기능 추가 전에 도구의 가치와 흐름을 재검토한다.

## 7. 최종 산출물과 인수 기준

최종 제출 구성:

1. 재현 가능한 전체 소스와 고정된 SDK/패키지 정보.
2. 실제 Windows에서 빌드한 self-contained 실행 ZIP 및 SHA-256.
3. `docs/quick-start.ko.md`: 실행, 단축키 두 개, 선택 메모, 지원 범위, 종료/삭제, 데이터 위치를 1페이지 분량으로 설명.
4. `docs/feasibility-report.md`: 활성 탭·Excel 연결 PoC 결과와 채택 근거.
5. `docs/validation-report.md`: 아래 템플릿에 실제 결과 기입.
6. `docs/compatibility.md`: OS·Excel 비트수/빌드·DRM/UNC별 검증 상태.
7. `docs/decisions.md`: 명세에서 달라진 구현 선택, 이유, 사용자 영향.

### 검증 보고서 템플릿

```markdown
# WorkBookmark 검증 보고서
- 검증일:
- 코드 커밋 / 실행파일 SHA-256:
- Windows 에디션·빌드 / Office 버전·비트수:
- 장비 / 사용자 권한 / DRM 여부:
- SDK / 패키지 lock:

| 시험 ID | 상태(통과/실패/미실행/차단) | 관찰 결과 | 증거 또는 재현 절차 |
|---|---|---|---|
| U01 | 미실행 | | |

## 자원·지연 측정
- 캡처 p95 / 최근 목록 p95 / 유휴 CPU / Private Bytes:
- 반복 횟수 / fixture / 측정 방법:

## 남은 제한
- 지원하지 않는 환경:
- 미실행 시험과 이유:
- 사용자에게 표시한 제한:

## 출시 판단
- 필수 시험 통과 여부:
- 정식 v1 / 제한된 시험판 / 소스만 준비 중 하나:
```

Windows/Office 실기 환경이 없는 Codex는 소스·규칙시험·가능한 빌드까지 진행하고, 실행 불가능한 단계만 정확히 남긴다. 이때 모의 결과를 실제 실행 결과로 포장하거나 테스트 보고서의 미실행을 통과로 바꾸지 않는다.
